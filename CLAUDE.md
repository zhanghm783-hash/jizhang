# 黑马记账 — 项目文档（CLAUDE.md）

> 本文档是「黑马记账」App 的产品需求文档 + 项目协作规则，由用户与 Claude Code 共同确定，全项目期有效。

## 一、项目简介

- **项目名称**：黑马记账
- **产品定位**：个人电脑桌面记账工具，帮助用户随手记录每一笔人民币花销，并查看消费统计。
- **运行平台**：Windows 和 Mac 电脑（桌面应用）。
- **核心特点**：轻量安装包、数据保存在本机、界面简洁易用。

## 二、协作规则（重要，全项目期遵守）

用户是**编程小白、首次使用 Claude Code**，因此 Claude 在本项目中的工作方式必须遵守以下规则：

1. **技术决策由用户拍板**：任何涉及技术方案的选择（选什么框架、用什么库、怎么做实现等），Claude 必须先列出多个可行方案，并用通俗易懂的大白话解释每个方案的优缺点，然后由用户选择决定。**禁止 Claude 擅自替用户做技术决策。**
2. **全程中文沟通**：所有交流、文档、界面文字使用中文。
3. **避免专业术语**：解释技术概念时，不用或尽量少用专业术语；必须用时，用生活中的类比解释清楚。
4. **每一步都解释**：每做一步操作，向用户说明「做了什么、为什么这么做」，让用户看得懂、跟得上。
5. **重大改动先确认**：涉及功能变更、数据格式改动、删改文件等重大操作，先征得用户同意再做。
6. **分阶段验收**：每完成一个开发阶段，向用户演示结果并汇报，经用户确认满意后再进入下一阶段。
7. **多问少猜**：产品功能、界面设计上的不确定之处，宁可列方案问用户，也不要自作主张。

## 三、产品需求

### 3.1 核心功能（一期 MVP）

1. **记一笔**：快速记录一笔花销。
2. **账单流水**：按日期分组查看所有花销记录，支持按月筛选。
3. **编辑 / 删除**：对记错的账单进行修改或删除（删除需二次确认）。
4. **本月汇总**：首页顶部显示本月总支出、笔数。
5. **统计图表**：分类占比饼图、近 6 个月消费趋势折线图、分类花费排行。
6. **本地保存**：所有数据保存在本机，不联网、不上传。

### 3.2 账单字段

每笔花销记录包含以下字段：

| 字段 | 说明 | 是否必填 |
| --- | --- | --- |
| 金额 | 人民币，单位：元，支持两位小数 | 必填 |
| 日期 | 默认当天，可修改 | 必填 |
| 一级分类 | 从分类树中选择 | 必填 |
| 二级分类 | 随一级分类联动选择 | 必填 |
| 支付方式 | 微信 / 支付宝 / 现金 / 银行卡 / 信用卡 / 其他 | 必填 |
| 备注 | 自由填写，如「请老王吃饭」 | 选填 |

### 3.3 分类体系（两级：一级大类 → 二级小类）

默认内置 10 个一级分类、50 个二级分类（一期内置，二期支持自定义增删改）：

1. **餐饮食品**：早餐、午餐、晚餐、零食饮料、水果、外卖点餐、请客聚餐、食材采购
2. **交通出行**：公交地铁、出租网约车、高铁火车、飞机、加油充电、停车费、共享单车、车辆保养
3. **购物消费**：服饰鞋包、数码家电、日用百货、美妆护肤、运动户外、宠物用品
4. **居住生活**：房租房贷、水电燃气、物业费、宽带话费、家居用品、家政维修
5. **娱乐休闲**：电影演出、游戏充值、旅游度假、运动健身、咖啡茶饮、会员订阅
6. **医疗健康**：门诊买药、体检保健、住院治疗、牙科眼科
7. **教育学习**：书籍文具、课程培训、考试报名、学费
8. **人情往来**：红包礼金、送礼、孝敬父母、公益捐款
9. **金融支出**：手续费、利息、罚金
10. **其他杂项**：其他

### 3.4 页面清单

1. **流水首页**：顶部「本月总支出」卡片；中部按日期分组的账单流水列表；底部/右下角「记一笔」大按钮；支持切换月份。
2. **记一笔弹窗**：金额输入 → 一级分类 → 二级分类（联动）→ 日期 → 支付方式 → 备注 → 保存。
3. **统计页**：本月支出总额；分类占比饼图；近 6 个月趋势折线图；分类花费排行（可切换月份）。

### 3.5 版本规划

- **一期（MVP，本次开发目标）**：记一笔、账单流水、编辑删除、本月汇总、统计图表、本地保存。
- **二期（增强）**：分类自定义管理（增删改分类）、账单搜索、导出 Excel/CSV、数据备份与恢复。
- **三期（可选）**：收入记录、月度预算提醒、云端同步（需重新评估存储方案）。

## 四、技术选型（已确定，2026-09-13 用户拍板）

### 4.1 技术栈明细

| 用途 | 选型 |
| --- | --- |
| 桌面应用框架 | Tauri 2（让网页界面变成电脑桌面程序的外壳） |
| 界面开发 | Vue 3 + Vite + TypeScript |
| 界面组件库 | Element Plus（现成的按钮、输入框等组件） |
| 状态管理 | Pinia |
| 统计图表 | ECharts |
| 本地数据库 | SQLite（通过 tauri-plugin-sql 使用） |

### 4.2 选择理由与代价（如实记录）

- **优点**：安装包只有约 10MB（对比 Electron 约 100MB）；内存占用低；启动快。
- **代价**：需要安装 Rust 编译环境；首次构建较慢；如果 Rust 环境出问题，排查起来比 Electron 麻烦一点。遇到问题 Claude 会负责排查解决。

### 4.3 开发环境（需要安装的软件，Claude 负责指导安装）

- **Node.js**（LTS 长期支持版）：界面部分的运行环境。
- **Rust**（通过 rustup 安装）：Tauri 的编译语言环境。
- **Microsoft C++ Build Tools**：Windows 上编译 Tauri 所需（勾选「使用 C++ 的桌面开发」工作负载）。
- **WebView2**：Windows 11 系统自带，无需额外安装。
- Mac 电脑上（打包 Mac 版时）需要：Xcode Command Line Tools + Node.js + Rust。

### 4.4 打包发布说明

- 在 Windows 电脑上打包出 Windows 安装包（.msi / .exe）。
- **Mac 安装包必须在 Mac 电脑上打包**（这是跨平台开发的通用限制，不是缺陷）。

## 五、数据存储

- 使用 **SQLite 本地数据库**，数据保存在用户自己电脑上的一个数据库文件中。
- 不联网、不上传、隐私安全；换电脑时拷贝该数据库文件即可迁移（二期将提供图形化备份/恢复功能）。
- 数据库文件位置由 Tauri 的应用数据目录管理（Windows：`%APPDATA%` 下；Mac：`~/Library/Application Support/` 下）。

## 六、开发流程约定

按以下阶段推进，**每阶段完成后演示给用户验收，确认后再进入下一阶段**：

1. **环境搭建**：指导用户安装 Node.js、Rust、C++ Build Tools，验证环境可用。
2. **项目骨架**：初始化 Tauri + Vue 项目，能启动出第一个窗口。
3. **记账功能**：数据库建表、记一笔弹窗、流水列表、编辑删除、本月汇总。
4. **统计功能**：饼图、趋势折线图、分类排行。
5. **打包发布**：打出 Windows 安装包，用户在自己电脑上安装试用。
6. **（如需要）Mac 版**：在 Mac 电脑上配置环境并打包 Mac 安装包。

---
*文档版本：v1.0（2026-09-13 创建）*
